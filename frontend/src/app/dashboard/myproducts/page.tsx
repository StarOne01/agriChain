'use client';

import SideBar from "../../../../component/Sidebar";

interface PageProps {}

const Page = ({}: PageProps) => {
    return (
        <div>
            <h1>Products</h1>
        </div>
    );
};

export default Page;